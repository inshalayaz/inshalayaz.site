import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
      // minWidth: 275,
      backgroundColor: '#000'
    },
   
    title: {
      fontSize: 14,
    },
    
  });

  export default useStyles